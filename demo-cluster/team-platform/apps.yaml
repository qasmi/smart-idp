- name: kagent-crds
  owner: platform
  project: platform
  namespace: kagent
  repo: ghcr.io/kagent-dev/kagent/helm
  chart: kagent-crds
  version: "*"
  autoSync:
    enabled: true
- name: kagent
  owner: platform
  project: platform
  namespace: kagent
  repo: ghcr.io/kagent-dev/kagent/helm
  chart: kagent
  valueFiles: 
    - kagent.yaml
  version: "0.6.15"
  autoSync:
    enabled: true
- name: kmcp-crd
  owner: platform
  project: platform
  namespace: kmcp-system
  repo: ghcr.io/kagent-dev/kmcp/helm
  chart: kmcp-crds
  version: "*"
  autoSync:
    enabled: true
- name: kmcp
  owner: platform
  project: platform
  namespace: kmcp-system
  repo: ghcr.io/kagent-dev/kmcp/helm
  chart: kmcp
  version: "*"
  autoSync:
    enabled: true