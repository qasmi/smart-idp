- name: kagent-crds
  owner: platform
  project: platform
  namespace: kagent
  repo: ghcr.io/kagent-dev/kagent/helm
  chart: kagent-crds
  version: "0.6.18"
  autoSync:
    enabled: true
- name: kagent
  owner: platform
  project: platform
  namespace: kagent
  repo: ghcr.io/kagent-dev/kagent/helm
  chart: kagent
  valueFile: kagent.yaml
  version: "0.6.18"
  autoSync:
    enabled: false
- name: kmcp-crd
  owner: platform
  project: platform
  namespace: kmcp-system
  repo: ghcr.io/kagent-dev/kmcp/helm
  chart: kmcp-crds
  version: "0.1.8"
  autoSync:
    enabled: true
- name: kmcp
  owner: platform
  project: platform
  namespace: kmcp-system
  repo: ghcr.io/kagent-dev/kmcp/helm
  chart: kmcp
  version: "0.1.8"
  autoSync:
    enabled: true
- name: jaeger
  owner: platform
  project: platform
  namespace: jaeger
  repo: https://jaegertracing.github.io/helm-charts
  chart: jaeger
  version: "*"
  valueFile: jaeger.yaml
  autoSync:
    enabled: true
